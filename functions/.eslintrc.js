/* eslint-env node */
module.exports = {
  env: { node: true, es2021: true },
  parserOptions: {
    ecmaVersion: 2022, // allow optional chaining, nullish coalescing, etc.
    sourceType: "script",
  },
  rules: {
    // keep or adjust rules as you like
  },
};
